const myLibrary = [];

function Book(title, page, author, year, genre, status) {
  // the constructor
  this.title = title;
  this.page = page;
  this.author = author;
  this.year = year;
  this.genre = genre;
  this.status = status;
}

function addBookToLibrary = (el) => {
  el.preventDefault();

  const titleVal =  
}
// eslint-disable-next-line no-console
console.log(myLibrary);
Book();
addBookToLibrary();